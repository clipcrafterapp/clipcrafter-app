/**
 * Remotion entry point — registers all compositions for bundling.
 * Keep this file import-only; no logic here.
 */
import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
